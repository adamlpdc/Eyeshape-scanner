/**
 * @deprecated Import from `@/constants/lash-recommendation-engine` instead.
 * Kept for backwards compatibility during refactors.
 */
export {
  LASH_RECOMMENDATION_ENGINE as EYE_SHAPE_BEAUTY,
  RECOMMENDATION_COPY as RESULTS_COPY,
} from "@/constants/lash-recommendation-engine";
